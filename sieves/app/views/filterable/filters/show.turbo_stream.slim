= turbo_stream.replace @model.filterable.turbo_frame_id,
                       render("filterable/filters/form", filters: @filters, filterable: @model.filterable, submit_path: filterable_params[:submit_path])
